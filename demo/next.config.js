/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/react-table-scroll',
    images: {
        unoptimized: true
    }
};

module.exports = nextConfig;
